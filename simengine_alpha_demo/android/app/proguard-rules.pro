# No Java/Kotlin code in this project (NativeActivity, hasCode=false) —
# nothing to keep. Placeholder so build.gradle.kts's proguardFiles()
# reference resolves; unused while isMinifyEnabled is false.
