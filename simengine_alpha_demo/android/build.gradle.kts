// android/build.gradle.kts
// Top-level build file: only declares the Android Gradle Plugin version
// used by :app (see app/build.gradle.kts). No repositories/dependencies
// live here — see settings.gradle.kts's dependencyResolutionManagement.
plugins {
    id("com.android.application") version "8.5.2" apply false
}
